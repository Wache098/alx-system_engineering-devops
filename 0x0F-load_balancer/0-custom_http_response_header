 script to configure a new Ubuntu machine with Nginx and custom settings

# Update package lists and install Nginx
sudo apt-get -y update
sudo apt-get -y install nginx

# Add a custom response header to Nginx configuration
HOST_NAME=$(hostname)
HEADER="\n\tadd_header X-Served-By $HOST_NAME;\n"
FIND=$(grep "X-Served-By" /etc/nginx/sites-available/default)
if [[ -z $FIND ]]; then
    sudo sed -i "23i $HEADER" /etc/nginx/sites-available/default
fi

# Create a simple index.html page
echo "Hello World!" | sudo tee /var/www/html/index.html

# Add a redirection rule to the Nginx configuration
REDIRECT_STRING="\n\tlocation /redirect_me {\n\t\treturn 301 https://www.youtube.com/watch?v=3MbaGJN2ioQ;\n\t}\n"
FIND_REDIRECT=$(grep "redirect_me" /etc/nginx/sites-available/default)
if [[ -z $FIND_REDIRECT ]]; then
    sudo sed -i "42i $REDIRECT_STRING" /etc/nginx/sites-available/default
fi

# Add a custom 404 error page to the Nginx configuration
ERROR_PAGE_STRING="\n\terror_page 404 /custom_404.html;\n"
FIND_ERROR_PAGE=$(grep "error_page 404" /etc/nginx/sites-available/default)
if [[ -z $FIND_ERROR_PAGE ]]; then
    echo "Ceci n'est pas une page" | sudo tee /var/www/html/custom_404.html
    sudo sed -i "40i $ERROR_PAGE_STRING" /etc/nginx/sites-available/default
fi

# Restart Nginx to apply the changes
sudo service nginx restart
